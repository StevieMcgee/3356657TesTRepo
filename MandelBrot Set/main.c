 #include <stdio.h>
 #include <math.h>
 #include <complex.h>
 double Z_real, z_real, Y_real, y_real, Z_img, z_img, Y_img, y_img,
 double w, x, y, z, c, a, b;
 int q;
 int maxx, maxy, count;
 double tempx, tempy;
 double zx, zy , yx, yy


 int main()
 {
    printf("Enter the two number to Test: \n");
    scanf("%f %f", &w, &x);
    w = y;
    x = z;
    c = w + x;
    while(zx*zx + zy*zy <4)&& count < MAXCOUNT{

        a= (y*y)-(z*z)+ w;
        b= (2*y*z)+x;


        Z_real = z_real*z_real - z_img*z_img + c;

        tempx =


        printf("iteration #%d = %0.4f\n", q, Z_real);
        q = q+1;

    }
    return 0;
 }
